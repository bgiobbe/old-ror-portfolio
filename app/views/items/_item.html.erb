<%#
# Render one row of library history table
%>

  <tr>
    <td><%=h Item::MEDIA_DESC[item.medium] %></td>
    <td><%= link_to h(item.title), item %></td>
    <td><%=h item.author %></td>
    <td><%=h item.checkout.strftime("%m-%d-%Y") %></td>
  </tr>
